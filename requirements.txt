# Core (also declared in pyproject.toml)
numpy>=1.24
opencv-python-headless>=4.8
pyyaml>=6.0
tqdm>=4.66
tyro>=0.8

# Full GPU stack is installed by apps/candle/setup.sh into the candle-scan conda env:
#   torch / torchvision, open3d, trimesh, transformers, Grounded-SAM-2, FoundationPose, …
# See apps/candle/environment.yml
