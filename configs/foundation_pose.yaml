# FoundationPose settings

extends: camera.yaml

mesh_path: data/meshes/cube.obj

foundation_pose:
  repo_root: external/FoundationPose
  checkpoint_dir: external/FoundationPose/weights
  refine_iterations: 2
  track_mode: true

rgbd_dir: data/rgbd/demo
mask_path: data/masks/frame_000000.png

output_dir: data/outputs
